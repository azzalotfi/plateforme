<!-- Header -->
{% block stylesheets %}
    <link href="{{ asset('css/register.css') }}" rel="stylesheet">
    <link href="{{ asset('css/profile.css') }}" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="{{ asset('css/app.css') }}" rel="stylesheet">
{% endblock %}
<header class="header">
    <div class="containerr">
        <div class="header-content">
            <a href="{{ path('home') }}" class="logo">
                <div class="logo-icon">
                    <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <rect width="40" height="40" rx="10" fill="url(#logoGradient)"/>
                        <path d="M12 20 L20 12 L28 20 M20 12 L20 28" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" opacity="0.9"/>
                        <defs>
                            <linearGradient id="logoGradient" x1="0" y1="0" x2="40" y2="40" gradientUnits="userSpaceOnUse">
                                <stop offset="0%" stop-color="#8b5cf6"/>
                                <stop offset="100%" stop-color="#ec4899"/>
                            </linearGradient>
                        </defs>
                    </svg>
                </div>
                <span class="logo-text">ServicePro</span>
            </a>
            <nav class="nav">
                <a href="{{ path('home') }}" class="nav-link {% if app.request.attributes.get('_route') == 'home' %}active{% endif %}" data-i18n="nav_home">Accueil</a>
                <a href="{{ path('services') }}" class="nav-link {% if app.request.attributes.get('_route') == 'services' %}active{% endif %}" data-i18n="nav_services">Services</a>
                <a href="{{ path('app_about') }}" class="nav-link {% if app.request.attributes.get('_route') == 'app_about' %}active{% endif %}" data-i18n="nav_about">À propos</a>
                <a href="{{ path('app_ai_assistant') }}" class="nav-link {% if app.request.attributes.get('_route') == 'app_ai_assistant' %}active{% endif %}" data-i18n="nav_ai_assistant">🤖 Assistant IA</a>
                {% if app.user and (app.user.role == 'prestataire' or 'ROLE_PRESTATAIRE' in app.user.roles) %}
                    <a href="{{ path('app_provider_dashboard') }}" class="nav-link {% if app.request.attributes.get('_route') == 'app_provider_dashboard' %}active{% endif %}" data-i18n="nav_dashboard">Tableau de bord</a>
                {% elseif app.user and (app.user.role == 'client' or 'ROLE_CLIENT' in app.user.roles) %}
                    <a href="{{ path('app_dashboard') }}" class="nav-link {% if app.request.attributes.get('_route') == 'app_dashboard' %}active{% endif %}" data-i18n="nav_dashboard">Tableau de bord</a>
                {% elseif app.user and (app.user.role == 'admin' or 'ROLE_ADMIN' in app.user.roles) %}
                    <a href="{{ path('app_admin_dashboard') }}" class="nav-link {% if app.request.attributes.get('_route') == 'app_admin_dashboard' %}active{% endif %}" data-i18n="nav_dashboard">Tableau de bord Admin</a>
                {% endif %}
            </nav>
            <div class="header-actions">
            </div>
        <nav class="nav">
            {% if app.user %}
                  <a href="{{ path('paiement') }}" data-i18n="nav_payment">Paiement</a>
           
            <div class="nav-right d-flex align-items-center">
                {{ render(controller('App\\Controller\\NotificationController::header')) }}
                
                {# Chat Icon #}
                <a href="{{ path('app_chat') }}" class="position-relative me-3" id="chatIcon" style="text-decoration: none; color: inherit;">
                    <i class="bi bi-chat-dots fs-4"></i>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success" id="chatUnreadBadge" style="font-size: 0.6rem; display: none;">
                        0
                        <span class="visually-hidden">unread messages</span>
                    </span>
                </a>
                
                <a href="{{ path('app_profile') }}" data-i18n="nav_profile">Mon Profil</a>
            </div>
             {% endif %}
        </nav>
                    {# User is not logged in - show login/register #}
                    <select id="langSelect" class="lang-toggle" style="padding:6px 10px;border:1px solid #e5e7eb;border-radius:8px;background:#fff;cursor:pointer;">
                        <option value="fr">Français</option>
                        <option value="en">English</option>
                    </select>
                    
                   {% if not app.user %}
    <a href="{{ path('app_login') }}" class="login-link" data-i18n="nav_login">Connexion</a>
    <a href="{{ path('app_register') }}" class="signup-btn" data-i18n="nav_register">S'inscrire</a>
{% endif %}
<nav class="nav">
            {% if app.user %}
              
                <a href="{{ path('app_logout') }}" class="signup-btn" data-i18n="nav_logout">Déconnexion</a>
            </div>
             {% endif %}
        </nav>
            </div>
        </div>
    </div>
</header>

<script>
// Poll for unread chat messages count
{% if app.user %}
function updateChatUnreadCount() {
    fetch('/chat/unread-count')
        .then(response => response.json())
        .then(data => {
            const badge = document.getElementById('chatUnreadBadge');
            if (data.count > 0) {
                badge.textContent = data.count;
                badge.style.display = 'inline-block';
            } else {
                badge.style.display = 'none';
            }
        })
        .catch(error => console.error('Chat count error:', error));
}

// Initial update and poll every 30 seconds
updateChatUnreadCount();
setInterval(updateChatUnreadCount, 30000);
{% endif %}
</script>


